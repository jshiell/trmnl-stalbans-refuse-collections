<div class="layout layout--row layout--stretch-x">
  <div class="layout layout--col">
    <div class="image image-stroke">{{ waste_logo }}</div>
  
    <div class="title">
        {% if next_refuse_date == now_date %}
            TODAY
        {% else %}
            {{ next_refuse.Next | date: "%A" }}
        {% endif %}
    </div>
    <div class="value value--xsmall">{{ next_refuse.Next | date: "%d/%m/%y" }}</div>
  </div>
  <div class="layout layout--col">
    <div class="image image-stroke">{{ recycling_logo }}</div>
  
    <div class="title">
        {% if next_recycling_date == now_date %}
            TODAY
        {% else %}
            {{ next_recycling.Next | date: "%A" }}
        {% endif %}
    </div>
    <div class="value value--xsmall">{{ next_recycling.Next | date: "%d/%m/%y" }}</div>
  </div>
  <div class="layout layout--col">
    <div class="image image-stroke">{{ food_logo }}</div>
  
    <div class="title">
        {% if next_food_date == now_date %}
            TODAY
        {% else %}
            {{ next_food.Next | date: "%A" }}
        {% endif %}
    </div>
    <div class="value value--xsmall">{{ next_food.Next | date: "%d/%m/%y" }}</div>
  </div>
  <div class="layout layout--col">
    <div class="image image-stroke">{{ garden_waste_logo }}</div>
  
    <div class="title">
        {% if next_garden_waste_date == now_date %}
            TODAY
        {% else %}
            {{ next_garden_waste.Next | date: "%A" }}
        {% endif %}
    </div>
    <div class="value value--xsmall">{{ next_garden_waste.Next | date: "%d/%m/%y" }}</div>
  </div>
</div>
<div class="title_bar">
  <div class="image image-stroke"><svg viewBox="0 0 24 24" style="height: 28px; width: 28px;"><path fill="currentColor" d="M20.37,8.91L19.37,10.64L7.24,3.64L8.24,1.91L11.28,3.66L12.64,3.29L16.97,5.79L17.34,7.16L20.37,8.91M6,19V7H11.07L18,11V19A2,2 0 0,1 16,21H8A2,2 0 0,1 6,19Z"></path></svg></div>
  <span class="title">St Albans Refuse Collections</span>
</div>
